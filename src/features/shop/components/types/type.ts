export type SelectedProductsProps = {
  title: string;
  subtitle: string;
  imageSrc: string;
  category: string;
  isTall?: boolean;
};

export type ProductDisplayProps = {
  title: string;
  sub: string;
};
